# Server
PORT=3000

# Database (PostgreSQL — e.g. Neon pooled connection string)
DATABASE_URL=postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=verify-full

# Auth — generate with: openssl rand -hex 32
JWT_SECRET=your-jwt-secret-here
